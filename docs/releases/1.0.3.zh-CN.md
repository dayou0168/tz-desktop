# TZ 1.0.3 LTO

> 本文件是未来 GitHub Release 的中文说明草稿，不代表 1.0.3 已完成构建、验收或发布。

## 发布内容

- Windows Release x64，正式构建配置必须启用 `DESKTOP_APP_ENABLE_LTO=ON`。
- 客户端固定连接主入口 `tztg.tianze8.cc:2398`。
- tag 约定：`1.0.3`。

## 计划发布文件

- `TZ-1.0.3-Setup.exe`
- `TZ-1.0.3-windows-x64.zip`
- `TZ-1.0.3-symbols.zip`
- `SHA256SUMS.txt`

## 发布前必须补齐

- 记录并核对最终发布 commit 与全部递归 gitlink。
- 完成全新 LTO Release 构建，并确认编译使用 `/GL`、链接使用 `/LTCG`。
- 核验安装程序、便携包和符号包内容及 `SHA256SUMS.txt`。
- 完成干净 Windows 环境中的安装、启动、登录、双客户端交互、断线恢复与卸载验收。
- 如产物仍未进行 Authenticode 数字签名，必须在正式 Release 中明确披露。

只有以上门禁全部完成后，才能以标题“TZ 1.0.3 LTO”创建 GitHub Release；不得把 Actions artifact 当作 GitHub Release。
